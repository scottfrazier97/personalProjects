```{r imports}
library(GGally)
library(tidyverse)
library(ggplot2)
```
mpg
fifa = read.csv(file.choose(), header = T)
diamonds
```{r categorical vars}
#count of each type of car
count(mpg, class)

#could not get this working
mpg %>% ggplot(aes(x=class fill=class)) + geom_bar() + geom_text(aes(class, totals$n + 2, label = totals$n, fill = NULL), data = totals)

```

```{r continuous vars}
mpg %>% ggplot(aes(x=cty, fill=class)) + geom_histogram()

mpg %>% ggplot(aes(x=cty, color=class)) + geom_freqpoly()

mpg %>% ggplot(aes(y=cty, color=class)) + geom_boxplot()

mpg %>% ggplot(aes(x=cty, color=class)) + geom_histogram() + facet_grid(rows = vars(class))
 
```

```{r EDA | two fundamental params}
#Center and spread
mpg %>% group_by(class) %>% summarize(mean = mean(cty), sd = sd(cty), median = median(cty), range = max(cty)-min(cty), IQR = IQR(cty), count = n())

```

```{r missing values}
##want to fill in empty cells with NA

df = data.frame(x = c(1,2,3,4), y =c(3,4,5,6), z = c(1,2,3,14))
df
sapply(diamonds,function(x) mean(x))

#Toy Example
# Bring in MissingValueToy.csv as ’a’
a = read.csv(file.choose(),header = TRUE)
a

# count missing values in each column
s = sapply(diamonds, function(x) sum(is.na(x)))
s

# Make all missing values NA
str(a)
a$chars[6] = NA
str(a)
a$chars = as.character(a$chars)
str(a)
library(naniar)
gg_miss_var(a)

#Fifa
gg_miss_var(fifa[,1:40]) + ylim(0,75)
gg_miss_var(fifa[,41:89]) + ylim(0,75)

#mpg
gg_miss_var(mpg) + ylim(0,10)
sapply(mpg, function(x) sum(is.na(x)))

#nycflights13
library(nycflights13)
gg_miss_var(flights)
sapply(flights, function(x) sum(is.na(x)))

```

gg_miss_var(diamonds[,1:40]) + ylim(0,75)

